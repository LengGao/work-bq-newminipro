<view class="page-body">
<view class="title">认证</view>
  <view class="page-body-wrapper">
    <camera device-position="front" flash="off" binderror="error" style="width: 100%; height: 320px;">
      <view class="mask-img">
        <view class="placeholder"></view>
        <image src="../../../../imgs/mask.png">
        </image>
        <view class="placeholder"></view>
        <view class="mask-text">
          <view class="mask-text-value">{{faceText}}</view>
        </view>
      </view>
    </camera>
    <view class="btn-area" wx:if="{{isError}}">
      <button type="primary" bindtap="faceStart">重试</button>
    </view>
    <!-- <view class="btn-area">
      <button type="primary" bindtap="takePhoto">拍照</button>
    </view> -->
    <!-- <view class="btn-area">
      <button type="primary" bindtap="startRecord">开始录像</button>
    </view>
    <view class="btn-area">
      <button type="primary" bindtap="stopRecord">结束录像</button>
    </view> -->
    <!-- <view class="preview-tips">预览</view> -->
    <!-- <image wx:if="{{src}}" mode="widthFix" src="{{src}}"></image>
    <video wx:if="{{videoSrc}}" class="video" src="{{videoSrc}}"></video> -->
  </view>
</view>